pipeline {
    agent any
    stages {
        stage('Clone https://github.com/prakash006602/webin.git ') {
            steps {
                git 'https://github.com//hotstar-website.git'
            }
        }
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t hotstar-website .'
            }
        }
        stage('Run Container') {
            steps {
                sh '''
                docker rm -f hotstar || true
                docker run -d --name hotstar -p 8081:8080 hotstar-website
                '''
            }
        }
    }
}

